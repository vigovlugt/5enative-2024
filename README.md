# 5enative 2024

```sh
bunx eas build --platform android --profile preview --local
```

or

```sh
nix develop
NODE_ENV=production bunx eas build --platform android --profile preview --local --output dist/app-preview.apk
```
